version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./app/storage/uploads:/app/storage/uploads
      - ./app/storage/models:/app/storage/models
    environment:
      - PORT=8000
    restart: unless-stopped
